#include<stdio.h>                                   
/**
 * main -Entry point
 * Description- prints out stdout
 * return void
 */
int main(void)
{
		printf("with proper grammar, but the outcome is a piece of art,");
		return (0);
}
